{
  "name": "cors-proxy",
  "version": "1.0.0",
  "description": "A micro server that handles CORS policy for API requests.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cors",
    "proxy"
  ],
  "author": "Christopher Scott",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1"
  }
}
